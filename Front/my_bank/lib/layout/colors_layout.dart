import 'package:flutter/material.dart';

class ColorsLayout {
  static primaryColor({opacity = 1}) =>
      Colors.blueGrey[800].withOpacity(opacity);
  static backgroundColor({opacity = 1}) =>
      Colors.blueGrey[700].withOpacity(opacity);
}
